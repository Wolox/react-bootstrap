module.exports = {
  extends: ['wolox-react', 'plugin:import/typescript'<% if (features.flow) { %>, 'plugin:flowtype/recommended'<% } %>]<% if (features.flow) { %>,
  plugins: ['flowtype']<% } %><% if (features.jest) { %>,
  env: {
    jest: true
  },
  settings:{
    react: {
      version: 'detect'
    }<% } %><% if (features['babel-module-resolver']) { %>,
    'import/resolver': {
      'babel-module': {
        alias: {
          '~components': './src/app/components',
          '~screens': './src/app/screens',
          '~config': './src/config',
          '~constants': './src/constants',
          '~redux': './src/redux',
          '~services': './src/services',
          '~utils': './src/utils'
        }
      }
    }
    <%} %>
  },
  rules: {
    // TODO: add this to eslint-config-wolox-react?
   'react/jsx-filename-extension': [1, { 'extensions': ['.js', '.jsx', '.tsx'] }]
  }
};
